# UI Refresh Backup Point

Backup created before the Egg Inc + Cookie Clicker Mobile style UI refresh.

Restore target:
- `src/App.jsx`

Restore command:
- Copy this folder's `App.jsx` back to `src/App.jsx`.

User command trigger:
- "백업포인트로 돌아가"
